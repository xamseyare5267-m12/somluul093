import 'dotenv/config';import axios from 'axios';
const url=String(process.env.SUPABASE_URL||'').replace(/\/$/,''),key=String(process.env.SUPABASE_SERVICE_ROLE_KEY||'');if(!url||!key)throw new Error('SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are required');const h={Authorization:`Bearer ${key}`,apikey:key,'Content-Type':'application/json'};const r=await axios.get(`${url}/rest/v1/app_state?id=eq.1&select=state`,{headers:h,timeout:15000});const state=r.data?.[0]?.state||{};let n=0;for(const [userId,w] of Object.entries(state.wallets||{})){await axios.post(`${url}/rest/v1/wallet_accounts`,{user_id:userId,balance:Number(w?.balance||0),coins:Math.max(0,Math.trunc(Number(w?.coins||0)))},{headers:{...h,Prefer:'resolution=merge-duplicates,return=minimal'},timeout:15000});n++;}console.log(`Migrated ${n} wallet accounts.`);
